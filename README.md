# FletBox

**Flet syntax for vanilla JavaScript**

Build web UIs with the same declarative syntax you love from Flet, but with zero dependencies and native DOM.

## Installation

```bash
npm install flet-box
